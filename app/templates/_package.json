{
  "name": "<%= algorithm %>",
  "private": true,
  "version": "0.0.0",
  "dependencies": {<% if (environment === "Node") { %>
      "chai": "~1.8.0"
  <% } %>},
  "scripts": {<% if (environment === "Node") { %>
    "test": "mocha ./spec/<%= file %>"
  <% } %>}
}
